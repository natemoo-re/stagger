{
    "name": "@stagger/root",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "start": "lerna run start",
        "build": "lerna run build",
        "test": "lerna run test"
    },
    "devDependencies": {
        "@types/jest": "^24.0.15",
        "lerna": "^3.16.1",
        "tsdx": "^0.7.2",
        "typescript": "^3.5.3"
    },
    "husky": {
        "hooks": {
            "pre-commit": "pretty-quick --staged"
        }
    },
    "prettier": {
        "printWidth": 80,
        "semi": true,
        "singleQuote": true,
        "trailingComma": "es5"
    }
}
